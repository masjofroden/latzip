//
//  AppRadius.swift
//  LatZip
//

import CoreGraphics

enum AppRadius {
    static let small: CGFloat = 6
    static let medium: CGFloat = 8
    /// Quick Look / placeholder en el panel derecho (entre `medium` y `large` del card).
    static let previewChrome: CGFloat = 12
    static let large: CGFloat = 14
    static let sheet: CGFloat = 16
    static let hero: CGFloat = 22
}
